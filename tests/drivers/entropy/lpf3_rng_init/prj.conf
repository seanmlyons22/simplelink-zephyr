CONFIG_ZTEST=y
# Disable the health tests so the driver's all-zeros content guard is the
# only gate under test. Per-family symbols; the inactive family's are
# harmlessly undefined for the board being built.
CONFIG_ENTROPY_CC23X0_RCT_ENABLED=n
CONFIG_ENTROPY_CC23X0_APT_ENABLED=n
CONFIG_ENTROPY_CC27XX_RCT_ENABLED=n
CONFIG_ENTROPY_CC27XX_APT_ENABLED=n

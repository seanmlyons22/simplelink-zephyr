CONFIG_ZTEST=y
CONFIG_PM=y

# Console on SEGGER RTT: survives standby (RAM-based) and frees the UART,
# which is suspended by system-managed device PM around each standby cycle.
CONFIG_UART_CONSOLE=n
CONFIG_USE_SEGGER_RTT=y
CONFIG_RTT_CONSOLE=y
CONFIG_SEGGER_RTT_BUFFER_SIZE_UP=8192
